// Configuration Next.js — rien de special pour l'instant
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // On pourra ajouter les domaines d'images autorises plus tard (Mapbox, S3, etc.)
};

export default nextConfig;
